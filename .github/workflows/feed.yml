name: Get Feed

on:
  schedule:
    - cron: '0 * * * *'        # 每小时的第0分钟运行
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install feedparser requests python-dotenv markdownify beautifulsoup4

      - name: Run feed
        env:
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
          RUN_FREQUENCY: 3600
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}   # 新增这一行
        run: python main.py
